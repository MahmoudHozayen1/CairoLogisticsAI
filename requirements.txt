# --- Core web framework (required) ---
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-WTF==1.3.0
email-validator==2.3.0
python-dotenv==1.2.2
gunicorn==26.0.0

# --- PostgreSQL driver (psycopg3 — used when DATABASE_URL points at Postgres) ---
psycopg[binary]==3.3.4

# --- Testing ---
pytest==9.0.3

# --- Documentation / presentation generator ---
python-pptx==1.0.2

# --- Optional: high-fidelity street routing for the optimizer ---
# Enable with ENABLE_STREET_ROUTING=1. Heavy dependencies; the optimizer falls
# back to fast straight-line geometry when these are not installed.
# osmnx==1.9.4
# scikit-learn==1.5.2
# numpy==2.1.3
# networkx==3.4.2
