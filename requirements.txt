# --- Core web framework (required) ---
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-WTF==1.3.0
email-validator==2.3.0
python-dotenv==1.2.2
gunicorn==26.0.0

# --- PostgreSQL driver (psycopg3 — used when DATABASE_URL points at Postgres) ---
psycopg[binary]==3.3.4

# --- Testing ---
pytest==9.0.3

# --- Documentation / presentation generator ---
python-pptx==1.0.2
python-docx==1.2.0

# --- Route optimization: graph algorithms (NetworkX TSP techniques) ---
# Unlocks the Christofides (1.5x guarantee) and Simulated Annealing strategies
# in app/routing/optimizer.py. The optimizer falls back to its pure-Python
# 2-opt sequencing if NetworkX is not installed, so this is recommended, not
# strictly required.
networkx==3.4.2

# --- Data-science benchmarking & reporting ---
# Used by scripts/benchmark_optimizer.py to regenerate the thesis-ready charts
# under docs/benchmarks/. Not needed for request/response web serving, but kept
# installed so the documented benchmark command works out of the box.
matplotlib==3.11.0

# --- Predictive ML layer (app/ml/) ---
# Powers the delivery-time / pickup-time / late-risk predictors, the demand &
# cost forecaster and the per-prediction reasoning surfaced on the AI dashboard.
# Pure-Python inference on CPU; artifacts are trained offline and loaded from
# instance/ml/artifacts/ at runtime, so this deploys on the free hosting tiers.
scikit-learn==1.9.0
pandas==3.0.3
joblib==1.5.3
# numpy & scipy are pulled in transitively (numpy also by matplotlib).

# --- Optional: high-fidelity street routing for the optimizer ---
# Enable with ENABLE_STREET_ROUTING=1. Heavy dependencies; the optimizer falls
# back to fast straight-line geometry when these are not installed.
# osmnx==1.9.4
# scikit-learn==1.5.2
# numpy==2.1.3
